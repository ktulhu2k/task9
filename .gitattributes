# Явно указываем, что shell-скрипты должны использовать LF
*.sh text eol=lf

# Опционально: все Python-файлы тоже в LF (стандарт для Python)
*.py text eol=lf

# Остальные текстовые файлы — по умолчанию (или CRLF на Windows)
* text=auto
